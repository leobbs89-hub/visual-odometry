# ============================================================
# Dependências de Deep Learning — Detectores Neurais
# Necessário apenas para: SUPERPOINT, LOFTR ou MATCHFORMER
#
# RECOMENDADO: use install.bat --neural
#   O script detecta automaticamente GPU NVIDIA e instala o
#   PyTorch com CUDA correto. GPU acelera significativamente.
#
# Instalação manual (CPU):
#   pip install -r requirements-neural.txt
#
# Instalação manual com GPU NVIDIA (CUDA 12.6):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#   pip install kornia>=0.7
#
# Outras versões CUDA: https://pytorch.org/get-started/locally/
# ============================================================

# Deep learning — CPU por padrão quando instalado via pip direto.
# install.bat --neural instala automaticamente a versão GPU se detectar NVIDIA.
torch>=2.1
torchvision>=0.16

# Feature matching neural — LoFTR
kornia>=0.7
